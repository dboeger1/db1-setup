[package]
name = "dboeger1-dotfiles"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.4.0", features = ["derive", "cargo"] }
lazy_static = "1.4.0"
path-trie-rs = { git = "ssh://git@github.com/dboeger1/path-trie.git" }
